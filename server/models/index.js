const { User } = require("./user");
const { Post } = require("./post");
const { Photo } = require("./photo");
const { Like } = require("./like");
const { Comment } = require("./comment");

module.exports = { User, Post, Photo, Like, Comment };
